import { Hero } from "../components/Hero"
import { Specials } from "../components/Specials"

export const HomePage = () => {
    return(
        <main>
            <Hero />
            <Specials />
        </main>
    )
}